# Ignore node_modules in root or any subfolders
node_modules/

# Ignore env files in both client and server
client/.env
server/.env

# Optional: if you ever use .env.local, .env.production, etc.
**/.env*